defmodule PleromaReduxWeb.MastodonAPI.MarkersController do
  use PleromaReduxWeb, :controller

  def index(conn, _params) do
    json(conn, %{})
  end

  def create(conn, _params) do
    json(conn, %{})
  end
end

